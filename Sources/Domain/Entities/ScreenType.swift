import Foundation

public enum ScreenType: String, Sendable, Codable {
    case login
    case dashboard
    case form
    case list
    case detail
    case settings
    case profile
    case search
    case other
}
